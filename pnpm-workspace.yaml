allowBuilds:
  esbuild: true

# Exempt our own first-party package from the 24h minimumReleaseAge
# guard so freshly-published versions install immediately. Matches by
# package name (no version suffix).
minimumReleaseAgeExclude:
  - '@guille-gallo/auth-kit'
